app:
  image: wedding_api:$APP_ENV
  environment:
    RAILS_ENV: development
  ports:
    - "80:80"
  links:
    - db
db:
  image: postgres:9.4
  environment:
    POSTGRES_USER: wedding_api
    POSTGRES_PASSWORD: thehorserunaway
  expose:
    - "5432"
